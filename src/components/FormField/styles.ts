import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  errorContainer: {
    height: 25,
  },
  errorText: {
    color: 'red',
    marginTop: 4,
  },
  labelText: {
    fontSize: 18,
    fontWeight: 'bold',
    marginVertical: 8,
  },
  input: {
    borderWidth: 1,
    borderColor: '#ccc',
    borderRadius: 15,
    padding: 20,
    width: '100%',
  },
});

export default styles;
